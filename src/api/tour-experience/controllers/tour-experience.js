'use strict';

/**
 * tour-experience controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::tour-experience.tour-experience');
